extends layout 
include mixins/_country_list

block content
    h2=title
    .country_wrapper 
        each country in allCountries 
            +countryList(country)
                
        else 
            There are no countries